# 🔧 本地开发说明

## 🌐 访问地址

### ✅ 推荐方式（使用 localhost）

启动开发服务器后，使用以下地址访问：

```
http://localhost:5173
```

或

```
http://127.0.0.1:5173
```

**重要**: Web Speech API 在 `localhost` 和 `127.0.0.1` 上可以使用 HTTP 协议，无需 HTTPS！

---

## 🚀 启动开发服务器

```bash
npm run dev
```

你会看到类似的输出：

```
  VITE v5.4.21  ready in 500 ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: http://192.168.2.248:5173/
  ➜  press h + enter to show help
```

---

## 📱 在手机上测试

### 方法 1：使用 USB 调试（推荐）

1. **启用 Chrome 远程调试**

   在电脑上访问：
   ```
   chrome://inspect/#devices
   ```

2. **连接手机**
   - 用 USB 连接手机到电脑
   - 在手机上启用 USB 调试
   - 在 Chrome DevTools 中选择你的设备

3. **端口转发**
   - 在 Chrome DevTools 中设置端口转发
   - 转发 `localhost:5173` 到手机
   - 在手机浏览器访问 `http://localhost:5173`

### 方法 2：构建 Android APK（推荐）

直接构建 APK 安装到手机测试：

```bash
# 初始化 Android 项目
npm run android:init

# 同步代码
npm run android:sync

# 在 Android Studio 中打开
npm run android:open

# 或直接构建 APK
npm run android:build
```

APK 位置: `android/app/build/outputs/apk/debug/app-debug.apk`

### 方法 3：使用局域网 IP（需要 HTTPS）

⚠️ **注意**: 如果使用局域网 IP（如 `http://192.168.2.248:5173`），Web Speech API **不会工作**，因为浏览器要求 HTTPS。

如果必须使用局域网 IP，需要配置 HTTPS：

#### 安装 mkcert（生成本地 HTTPS 证书）

**Windows (PowerShell 管理员):**
```powershell
# 使用 Chocolatey 安装
choco install mkcert

# 或使用 Scoop 安装
scoop bucket add extras
scoop install mkcert

# 安装本地 CA
mkcert -install
```

**macOS:**
```bash
brew install mkcert
mkcert -install
```

**Linux:**
```bash
# Debian/Ubuntu
sudo apt install libnss3-tools
wget https://github.com/FiloSottile/mkcert/releases/download/v1.4.4/mkcert-v1.4.4-linux-amd64
chmod +x mkcert-v1.4.4-linux-amd64
sudo mv mkcert-v1.4.4-linux-amd64 /usr/local/bin/mkcert
mkcert -install
```

#### 生成证书

在项目根目录运行：

```bash
# 生成证书（替换为你的局域网 IP）
mkcert localhost 127.0.0.1 192.168.2.248

# 会生成两个文件：
# - localhost+2.pem (证书)
# - localhost+2-key.pem (私钥)
```

#### 修改 vite.config.ts

```typescript
import fs from 'fs'

export default defineConfig({
  // ... 其他配置
  server: {
    host: '0.0.0.0',
    port: 5173,
    https: {
      key: fs.readFileSync('./localhost+2-key.pem'),
      cert: fs.readFileSync('./localhost+2.pem'),
    }
  }
})
```

#### 在手机上安装证书

1. 将 `mkcert` 的根证书传输到手机
   - 证书位置: `mkcert -CAROOT` 命令查看
   - 通常在 `~/.local/share/mkcert/` (Linux/macOS) 或 `%LOCALAPPDATA%\mkcert\` (Windows)

2. 在手机上安装证书
   - Android: 设置 → 安全 → 加密与凭据 → 从存储设备安装
   - iOS: 设置 → 通用 → VPN与设备管理 → 安装描述文件

3. 访问 `https://192.168.2.248:5173`

---

## 🔍 故障排除

### 问题 1: "此站点的连接不安全" (ERR_SSL_VERSION_OR_CIPHER_MISMATCH)

**解决方案**: 使用 `http://localhost:5173` 而不是局域网 IP

### 问题 2: "浏览器不支持语音识别"

**原因**: 
- 使用了 HTTP 协议访问局域网 IP
- 浏览器不是 Chrome 或 Edge

**解决方案**:
- 使用 `http://localhost:5173` 访问
- 或配置 HTTPS（见上文）
- 或直接构建 APK 测试

### 问题 3: "无法访问麦克风"

**解决方案**:
1. 检查浏览器权限设置
2. 确保使用 HTTPS 或 localhost
3. 检查系统麦克风权限

### 问题 4: 局域网其他设备无法访问

**检查防火墙**:

Windows:
```powershell
# 允许端口 5173
netsh advfirewall firewall add rule name="Vite Dev Server" dir=in action=allow protocol=TCP localport=5173
```

macOS/Linux:
```bash
# 检查防火墙设置
sudo ufw allow 5173
```

---

## 📊 浏览器支持

### Web Speech API 支持情况

| 浏览器 | HTTP (localhost) | HTTP (局域网) | HTTPS |
|--------|------------------|---------------|-------|
| Chrome | ✅ | ❌ | ✅ |
| Edge   | ✅ | ❌ | ✅ |
| Firefox| ❌ | ❌ | ❌ |
| Safari | ❌ | ❌ | ❌ (iOS) |

**推荐**: 使用 Chrome 或 Edge 浏览器

---

## 🎯 最佳实践

### 本地开发

```bash
# 启动开发服务器
npm run dev

# 在浏览器访问
http://localhost:5173
```

### 手机测试

```bash
# 构建 Android APK
npm run android:init
npm run android:sync
npm run android:build

# 安装到手机
adb install android/app/build/outputs/apk/debug/app-debug.apk
```

### 生产部署

```bash
# 构建生产版本
npm run build

# 推送到 GitHub，自动构建 APK
git push origin main
```

---

## 💡 提示

1. **开发时使用 localhost**: 最简单，无需配置 HTTPS
2. **手机测试使用 APK**: 最接近真实使用场景
3. **生产部署使用 GitHub Actions**: 自动构建，省时省力

---

## 🎉 快速开始

```bash
# 1. 安装依赖
npm install

# 2. 启动开发服务器
npm run dev

# 3. 在浏览器访问
# http://localhost:5173

# 4. 开始开发！
```

**祝你开发愉快！** 🚀


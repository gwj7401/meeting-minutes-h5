# 📦 会议纪要 - 部署指南

## ✅ 项目状态

- ✅ Kotlin Android 版本已删除
- ✅ TypeScript + Vue 3 H5 版本已完成
- ✅ 所有源代码文件已创建
- ✅ 构建测试通过
- ✅ GitHub Actions 配置完成 (JDK 17)

---

## 🚀 部署步骤

### 1. 推送代码到 GitHub

```bash
# 初始化 Git 仓库（如果还没有）
git init

# 添加所有文件
git add .

# 提交
git commit -m "feat: 完成 H5 会议纪要应用 - 免费实时语音转文字"

# 添加远程仓库（替换为你的仓库地址）
git remote add origin https://github.com/你的用户名/会议纪要.git

# 推送到 GitHub
git push -u origin main
```

### 2. GitHub Actions 自动构建

推送代码后，GitHub Actions 会自动：

1. ✅ 检出代码
2. ✅ 设置 Node.js 20
3. ✅ 设置 JDK 17
4. ✅ 安装依赖
5. ✅ 构建 Web 应用
6. ✅ 初始化 Capacitor Android
7. ✅ 构建 APK
8. ✅ 上传 APK 到 Artifacts
9. ✅ 创建 Release

### 3. 下载 APK

#### 方法 1: 从 Artifacts 下载

1. 进入 GitHub 仓库
2. 点击 **Actions** 标签
3. 选择最新的成功构建
4. 在页面底部找到 **Artifacts** 区域
5. 下载 `app-release`
6. 解压得到 `app-release-unsigned.apk`

#### 方法 2: 从 Releases 下载

1. 进入 GitHub 仓库
2. 点击 **Releases** 标签
3. 下载最新版本的 APK

### 4. 安装到手机

1. 将 APK 传输到 Android 手机
2. 在手机上打开 APK 文件
3. 允许安装未知来源应用
4. 安装完成

---

## 🌐 Web 部署

### 部署到 GitHub Pages

1. **创建 GitHub Pages 分支**

```bash
# 构建项目
npm run build

# 部署到 gh-pages 分支
npx gh-pages -d dist
```

2. **配置 GitHub Pages**

- 进入仓库 Settings
- 找到 Pages 设置
- Source 选择 `gh-pages` 分支
- 保存

3. **访问网站**

访问 `https://你的用户名.github.io/会议纪要/`

### 部署到 Vercel

1. 访问 [vercel.com](https://vercel.com)
2. 导入 GitHub 仓库
3. 构建命令: `npm run build`
4. 输出目录: `dist`
5. 部署

### 部署到 Netlify

1. 访问 [netlify.com](https://netlify.com)
2. 导入 GitHub 仓库
3. 构建命令: `npm run build`
4. 发布目录: `dist`
5. 部署

---

## 📱 本地测试

### Web 版本

```bash
# 开发模式
npm run dev

# 访问 https://localhost:5173
```

### Android 版本

```bash
# 初始化 Android 项目
npm run android:init

# 同步代码
npm run android:sync

# 在 Android Studio 中打开
npm run android:open

# 或直接构建 APK
npm run android:build
```

APK 位置: `android/app/build/outputs/apk/release/app-release-unsigned.apk`

---

## 🔧 配置说明

### 修改应用信息

编辑 `capacitor.config.ts`:

```typescript
const config: CapacitorConfig = {
  appId: 'com.meetingminutes.app',  // 修改为你的应用 ID
  appName: '会议纪要',               // 修改为你的应用名称
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  },
  android: {
    buildOptions: {
      releaseType: 'APK'
    }
  }
}
```

### 修改应用图标

1. 准备图标文件 (1024x1024 PNG)
2. 使用在线工具生成各种尺寸: [icon.kitchen](https://icon.kitchen)
3. 替换 `android/app/src/main/res/` 下的图标文件

### 签名 APK

1. **生成密钥库**

```bash
keytool -genkey -v -keystore my-release-key.jks -keyalg RSA -keysize 2048 -validity 10000 -alias my-key-alias
```

2. **配置签名**

编辑 `android/app/build.gradle`:

```gradle
android {
    signingConfigs {
        release {
            storeFile file('../../my-release-key.jks')
            storePassword 'your-password'
            keyAlias 'my-key-alias'
            keyPassword 'your-password'
        }
    }
    buildTypes {
        release {
            signingConfig signingConfigs.release
        }
    }
}
```

3. **构建签名 APK**

```bash
cd android
./gradlew assembleRelease
```

---

## 📊 构建产物

### Web 构建

```
dist/
├── index.html
├── assets/
│   ├── *.css
│   └── *.js
└── vite.svg
```

大小: ~300 KB (gzip 压缩后)

### Android APK

```
android/app/build/outputs/apk/release/
└── app-release-unsigned.apk
```

大小: ~5-10 MB

---

## 🔍 故障排除

### 构建失败

1. **清理缓存**

```bash
rm -rf node_modules package-lock.json
npm install
```

2. **清理构建产物**

```bash
rm -rf dist android
npm run build
```

### Android 构建失败

1. **检查 JDK 版本**

```bash
java -version  # 应该是 JDK 17
```

2. **清理 Gradle 缓存**

```bash
cd android
./gradlew clean
```

### 语音识别不工作

1. **检查浏览器支持**
   - 使用 Chrome 或 Edge 浏览器
   - 不支持 Firefox 和 Safari (iOS)

2. **检查 HTTPS**
   - Web Speech API 需要 HTTPS
   - 本地开发使用 `https://localhost`

3. **检查麦克风权限**
   - 允许浏览器访问麦克风
   - 检查系统麦克风设置

---

## 📝 下一步

1. ✅ **推送代码到 GitHub**
2. ✅ **等待 GitHub Actions 构建完成**
3. ✅ **下载并测试 APK**
4. 🔄 **根据需要调整 UI 和功能**
5. 🔄 **添加应用图标和启动画面**
6. 🔄 **签名 APK 用于发布**
7. 🔄 **发布到应用商店（可选）**

---

## 🎉 完成！

你现在拥有了一个完整的、可部署的会议纪要应用！

- ✅ 免费实时语音转文字
- ✅ 跨平台支持 (Web + Android)
- ✅ 自动化构建和部署
- ✅ 隐私保护（本地存储）

**祝你使用愉快！** 🎊

